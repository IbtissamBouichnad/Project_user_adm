sap.ui.define([
    "./BaseController"
], function(BaseController) {
    "use strict";

    return BaseController.extend("projectusersadm.controller.Roles", {
        onInit: function() {
            // Initialisation de la vue Roles
        }
    });
});